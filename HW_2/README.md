# HW_2 «Кибер-Зоопарк 2077»

## ⏱️ Дедлайн – 06.12.2025 23:59

---

## 📌 Постановка задачи

Представьте, что вам поручили разработать интерфейс для виртуального Кибер-Зоопарка 2077, где обитают цифровые животные — гибриды биологии и искусственного интеллекта. Пользователь может наблюдать за питомцами, менять их характеристики и настроение, кормить их, «обновлять прошивки» и общаться с ними через контекстное меню.

**Задача:** разработать одностраничное приложение CyberZoo Dashboard, позволяющее управлять виртуальными животными, применять к ним различные действия, а также отображать статистику и системные события.

---

## ✨ Функциональные требования

### Загрузка данных
- Загрузить список питомцев из `pets.json` или мокового API с задержкой.
- Отобразить Skeleton Loader во время загрузки.
- Отобразить питомцев в сетке (Grid).
- Добавить возможность отфильтровать питомцев (см. [Фильтрация](#Фильтрация))

### Карточка питомца (PetCard)
- Управление состоянием одного питомца через **useReducer**.
- Каждый питомец имеет: `id`, `name`, `species`, `mood`, `energy`, `level`, `avatar`.
- Доступные действия:
    - `Feed` (+energy)
    - `Level Up` (повышает уровень)
    - `Cheer` / `Pet` (улучшает mood)
    - `Reset` (сброс до исходного состояния)
- Каждое действие создаёт запись в глобальном логе событий.
- Компонент должен быть оптимизирован через **React.memo**.
- Колбэки — мемоизированы через useCallback.
- Эффект для аватара через useRef.

### Таймер активности (useEffect + custom hook)
- Раз в **N** секунд автоматически уменьшать энергию.
- Различные `mood` при различных `energy` (градацию можете придумать сами)
- При `energy <= 20` настроение становится sad.
- При `energy = 0` питомец становиться недоступным.
- Логика вынесена в кастомный хук `usePetLifecycle`.

### Глобальный лог событий (useContext)
- Контекст хранит массив строк-событий.
- Возможность добавлять и очищать события.
- Отображение лога в боковой панели (Material UI Drawer/Paper).
- Кастомный хук `useEventLog` для работы с логами.

### Фильтрация
- Фильтр питомцев по виду (species).
- Реализовать через Material UI Select.
- Мемоизация списка через useMemo.

### Стилизация

*Каждый тип стилизации должен быть использован хотя бы один раз.*

1. SCSS (глобальный файл) — базовая сетка приложения.

2. module.scss — стили карточки питомца.

3. inline styles — микростили (например, динамическая тень в зависимости от mood).

4. styled-components — оформления кнопок взаимодействия.

5. Material UI:
    - панель событий,
    - кнопки фильтрации животных,
    - Skeleton Loader.

---

## Рекомендуемая структура приложения
```
src/
 ├─ components/
 │   ├─ PetCard/
 │   │    ├─ PetCard.tsx
 │   │    ├─ PetCard.module.scss
 │   │    └─ types.ts
 │   ├─ PetActions/
 │   │    └─ ActionButton.styled.ts   // styled-components
 │   └─ EventLog/
 │        └─ EventLog.tsx
 │
 ├─ context/
 │    └─ EventContext.tsx
 │
 ├─ hooks/
 │    ├─ usePetLifecycle.ts
 │    └─ useEventLog.ts
 │
 ├─ data/
 │    └─ pets.json
 │
 ├─ pages/
 │    └─ Dashboard.tsx
 │
 ├─ styles/
 │    └─ global.scss
 │
 ├─ App.tsx
 └─ main.tsx (или index.tsx)
```
---

## ✅ Критерии приёмки

| Категория            | Требование                                           | Баллы  |
|----------------------|------------------------------------------------------|--------|
| **Архитектура и TS** | Полная типизация, корректная структура директорий    | **5**  |
| **Хуки React**       | useState + useEffect, useRef для DOM-эффектов        | **5**  |
|                      | useReducer в карточке                                | **10** |
|                      | Оптимизация через useMemo + useCallback и React.memo | **5**  |
|                      | 2 кастомных хука                                     | **10** |
| **Функциональность** | Загрузка, отображение, фильтрация                    | **5**  |
|                      | Таймер энергии                                       | **5**  |
|                      | Лог событий -> useContext + EventContext             | **10** |
|                      | Рабочие действия питомца                             | **5**  |
| **Стилизация**       | SCSS + module.scss                                   | **5**  |
|                      | Inline styles                                        | **5**  |
|                      | styled-components                                    | **5**  |
|                      | Material UI                                          | **5**  |
| **Итого:**           | Максимальный балл                                    | **80** |

### Использование `any` и `//@ts-ignore` или неработающее приложение = **0 баллов** за все ДЗ
